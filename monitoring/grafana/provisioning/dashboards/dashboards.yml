apiVersion: 1

providers:
  - name: "vocab-cat-dashboards"
    orgId: 1
    folder: "Vocab CAT"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
